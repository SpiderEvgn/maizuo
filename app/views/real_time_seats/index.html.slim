.container-fluid
  .row
    .col-lg-12
      h1 来自卖座网的影院座位信息［测试］
      = link_to '影院列表', cinemas_path
      table.table.table-striped.table-hover
        thead
          tr
            / th 
            / th 
            / th 
            / th  
            / th 
            / th 
            th Seats

        tbody
          - @realTimeSeats.each do |realTimeSeat|
            tr
              / td = link_to admin_employee_applicant_path(applicant) do
              /   = applicant.employee_snum
              / 用于将来做编号的链接
              / td = image_tag(film.picAddr, height: '100', width: '100')
              / td = realTimeSeat.
              / td = realTimeSeat.
              / td = realTimeSeat.
              / td = realTimeSeat.
              / td = realTimeSeat.
              td = realTimeSeat

      / = will_paginate @realTimeSeats, renderer: BootstrapPagination::Rails